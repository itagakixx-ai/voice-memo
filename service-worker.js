const CACHE_NAME="voice-memo-v1",APP_FILES=["./","./index.html","./styles.css","./app.js","./manifest.webmanifest"];
self.addEventListener("install",event=>{event.waitUntil(caches.open(CACHE_NAME).then(cache=>cache.addAll(APP_FILES)));self.skipWaiting()});self.addEventListener("activate",event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE_NAME).map(key=>caches.delete(key)))));self.clients.claim()});self.addEventListener("fetch",event=>{if(event.request.method!=="GET")return;event.respondWith(caches.match(event.request).then(cached=>cached||fetch(event.request)))});
self.addEventListener("push",event=>{let message={};try{message=event.data?event.data.json():{}}catch{}const title=typeof message.title==="string"&&message.title.trim()?message.title:"声メモ",body=typeof message.body==="string"&&message.body.trim()?message.body:"新しい通知があります。";event.waitUntil(self.registration.showNotification(title,{body,tag:"voice-memo-push",data:{url:self.registration.scope}}))});
self.addEventListener("notificationclick",event=>{event.notification.close();const appUrl=self.registration.scope;event.waitUntil(self.clients.matchAll({type:"window",includeUncontrolled:true}).then(async windowClients=>{const existingClient=windowClients.find(client=>client.url.startsWith(appUrl));if(existingClient&&"focus"in existingClient)return existingClient.focus();if(self.clients.openWindow)return self.clients.openWindow(appUrl)}))});
